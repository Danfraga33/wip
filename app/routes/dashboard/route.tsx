import React from 'react';
import { SidebarComponent } from '~/components/ui/SideBarComp';

const Dashboard = () => {
	return (
		<div className="flex h-screen items-center justify-center w-screen">
			<SidebarComponent />
		</div>
	);
};

export default Dashboard;
